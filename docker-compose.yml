services:
  murimstudio:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - app_data:/app/data
      - ./output:/app/output
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/app.db
    restart: unless-stopped

volumes:
  app_data:
