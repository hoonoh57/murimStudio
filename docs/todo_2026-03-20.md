# 📋 TODO — murimStudio 전 영역 A등급 달성 (2026-03-20)

> 이 파일이 모든 작업의 **유일한 기준**입니다.
> 작업은 Phase 순서대로 진행하며, 각 Phase 내에서도 번호순으로 진행합니다.
> 이미 완료된 항목은 `[x]`로 표시되어 있습니다.

---

## ✅ 완료된 작업

### 인프라 / 구조
- [x] 프로젝트 초기 구조 (v1.0 ~ v1.4)
- [x] NiceGUI 10개 탭 구성 (대시보드~비용)
- [x] SQLite DB (aiosqlite) 연동 + scripts 테이블 format/genre/target_duration 컬럼 (v1.7)
- [x] Dockerfile / docker-compose.yml
- [x] PROJECT_OVERVIEW.md 작성

### 트렌드 & 스크립트
- [x] 트렌드 수집 (네이버/Reddit/MAL), AI 랭킹, 레퍼런스 수집기
- [x] 스크립트 생성/번역 엔진, 다국어 번역 (ko/en/id/th)
- [x] 숏츠 전용 SYSTEM_PROMPT (HOOK→PROBLEM→SOLUTION→CTA + [SFX]/[자막]/[영상] 태그) (v1.7.1)
- [x] 롱폼 SYSTEM_PROMPT에 [SFX]/[자막] 태그 추가 (v1.7.1)

### TTS
- [x] Edge TTS 서비스 (20+ 음성 모델, 다국어)
- [x] TTS 제어문 필터링 강화 (v1.7.1), 괄호영어/번역주석 완전 제거 (v1.7.2)
- [x] 스크립트 씬별 파싱 엔진 extract_scenes (v1.7.1)

### 이미지
- [x] Pollinations.ai FLUX 이미지 생성, 장르별 스타일 프리셋 8종, 장르 자동 감지
- [x] format → `fmt` 리네임 (v1.7.2), 숏츠 9:16 / 롱폼 16:9 해상도 분기 (v1.7)
- [x] 인메모리 캐시 시스템, overwrite, delete_image/delete_all_images/delete_script_folder (v1.7.2)

### 영상 & 숏츠
- [x] FFmpeg 슬라이드쇼 + 페이드 전환 (롱폼), Ken Burns 5종
- [x] FFmpeg Ken Burns 필터 수정 (적응형 스케일+pad), 폴백 클립, 경로 통일 (v1.6.1)
- [x] 숏츠 패널 UI (v1.6)

### 제작물 관리
- [x] asset_browser.py 삭제 UI, format/genre 배지, shorts 디렉토리 통합 (v1.7.2)

### 리서치 (완료)
- [x] 바이럴 숏츠 제작 기법 리서치 (알고리즘, 훅 10종, 리텐션 기법, GAP 분석)
- [x] 무료 이미지 모델 조사 (Pollinations 전체 카탈로그: flux, zimage, klein, gptimage, dirtberry, imagen-4, kontext)
- [x] 무료 TTS 조사 (Qwen3-TTS 한국어 10개국어, GPT-SoVITS v3 한국어, FishAudio S1)
- [x] SFX/BGM 조사 (ElevenLabs Sound Effects, Stable Audio Open, Freesound.org)
- [x] GPU RTX 4070 8GB 활용범위 확정 (Qwen3-TTS 1.7B ~6GB, Stable Audio ~4GB, Real-ESRGAN ~1GB, Wan2.2 GGUF 8GB)
- [x] YouTube 2026 AI slop 정책 조사 — CEO 공식 발표, 수천 채널 수익화 정지, 슬라이드쇼+합성음성 = AI slop 판정
- [x] AI 비디오 생성 모델 조사 — Pollinations.ai wan/ltx-2/seedance, Puter.js 무료 Wan 2.2, Hailuo AI
- [x] FLUX Kontext 캐릭터 일관성 기법 조사 — 참조 이미지, 멀티샷 일관성
- [x] PyCaps Python 키네틱 자막 라이브러리 조사 — CSS 스타일, 단어별 하이라이트, 오픈소스
- [x] ComfyUI 활용 조사 — 헤드리스 API, Python 통합, FLUX Kontext/Wan2.2/Real-ESRGAN 워크플로우

### 커밋 히스토리
- [x] v1.6.1 `05da7e4` → v1.7 `b614dbe` → v1.7.1 `3bd7d89` → v1.7.2 `f929703` → v1.7.2 `767a8de`

---

## 🎯 품질 현황 & 목표

| 영역 | 현재 등급 | 핵심 문제 | 목표 등급 |
|---|---|---|---|
| 이미지 | **D** | 캐릭터 불일관, AI 결함, 단일 모델 | **A** |
| TTS | **C-** | 기계적 Edge TTS, 감정 없음, 3개 음성 | **A** |
| 영상 | **D+** | 정적 슬라이드쇼 = YouTube AI slop | **A** |
| SFX/BGM | **없음** | 무음 = 이탈, 단일 TTS 트랙 | **A** |
| 자막 | **D** | ASS 고정, 단일 스타일, 타이밍 부정확 | **A** |
| 스크립트 | **B-** | 태그가 영상으로 미변환 | **A** |
| 파이프라인 | **C** | 탭 단절, 대량 에러, 수동 | **A** |

---

## 🔴 Phase A — 영상 혁명 (최우선, YouTube AI slop 탈출)

> 정적 슬라이드쇼 → 실제 움직이는 영상 클립. 이것만으로 AI slop 판정 벗어남.
> GPU 불필요 (API 기반) 또는 ComfyUI 로컬.

### A-1. AI 비디오 클립 생성 서비스 ← ✅ 코어 완료 (v1.8.0)
> 파일: `app/services/video_clip_service.py` ← 신규 생성 완료

- [x] `VideoClipService` 클래스 생성
- [x] 이미지 → 3~5초 AI 동영상 변환 (정적 이미지에 자연스러운 움직임 부여)
- [x] **Pollinations.ai 비디오 API 통합**:
  - `wan` 모델 (Alibaba, 이미지→영상, 오디오 포함, $0.05/초)
  - `ltx-2` 모델 (빠른 생성, 오디오 포함, $0.01/초)
  - `seedance` 모델 (ByteDance 고품질 모션, $0.024/초)
  - 엔드포인트: `GET https://gen.pollinations.ai/video/{prompt}?model=wan&duration=4`
- [ ] **무료 폴백 체인**:
  - Puter.js API (Wan 2.2 T2V + Seedance 무료·무제한)
  - Hailuo AI (MiniMax 무료 티어, 워터마크)
- [x] **Ken Burns 6종 확장 폴백** (API 전부 실패 시 FFmpeg 로컬)
- [ ] **ComfyUI 로컬 폴백** (GPU 활용 시):
  - Wan 2.2 5B GGUF 모델 (8GB VRAM 적합, Q5 양자화)
  - ComfyUI 헤드리스 API 호출 (`http://localhost:8188/prompt`)
  - 480p 해상도로 생성 → Real-ESRGAN으로 업스케일
- [x] 프롬프트 자동 생성: 이미지 프롬프트 + 장르별 모션 프리셋 5종
- [ ] shorts_maker.py 연동: 기존 정적 이미지 대신 AI 비디오 클립 사용 옵션
- [ ] video_assembler.py 연동: 롱폼도 비디오 클립 사용 가능

### A-2. 키네틱 자막 시스템 (PyCaps)
> 파일: 신규 `app/services/caption_service.py`, shorts_maker.py 수정

- [ ] `pip install pycaps` (CSS 스타일 애니메이션 자막 Python 라이브러리)
- [ ] `CaptionService` 클래스 생성
- [ ] 기존 ASS 고정 자막 → PyCaps 키네틱 자막 전환
- [ ] 단어별 하이라이트: 핵심 단어 노란색/빨간색 색상 변경
- [ ] 팝업 애니메이션: 단어가 아래→위로 등장
- [ ] 크기 변화: 강조 단어 1.3배 확대
- [ ] **ASS 카라오케 폴백** (PyCaps 불안정 시):
  - ASS `\k` (karaoke) 태그 + `\t` (animation) 태그 조합
  - `\k100\c&H00FFFF&단어` → 단어별 하이라이트
  - FFmpeg `drawtext` 기반 워드 단위 시간 제어
- [ ] 자막 스타일 프리셋: 무협(금색+검정), 코미디(흰색+밝은톤), 호러(빨강+어둡)

### A-3. 3-layer 오디오 믹싱
> 파일: shorts_maker.py, video_assembler.py 수정

- [ ] FFmpeg `amix` 필터로 3트랙 합성:
  - TTS 나레이션: 100% 볼륨
  - BGM 배경음악: 15~20% 볼륨 (나레이션 구간 자동 덕킹)
  - SFX 효과음: 50% 볼륨 (이벤트성, 타임코드 기반)
- [ ] 기본 BGM 라이브러리: 장르별 로열티 프리 트랙 5곡 이상 포함 (`static/bgm/`)
- [ ] SFX 기본 세트: 검 소리, 폭발, 우쉬, 임팩트, 라이저 (`static/sfx/`)
- [ ] 나레이션 무음 구간 자동 감지 → BGM 볼륨 자동 상승
- [ ] 숏츠 UI에 BGM 선택 드롭다운, SFX on/off 토글

### A-4. 시각 효과 확장 + 씬 전환
> 파일: shorts_maker.py 수정

- [ ] Ken Burns 5종 → 12종+: zoom_bottom, pan_up, pan_down, zoom_out_reveal, shake_impact, flash_transition, split_zoom, rotate_slow
- [ ] **씬 간 xfade 전환 효과**: fade(0.3초), wipeleft, slidedown, radial, pixelize
- [ ] **첫 프레임 훅 오버레이**: 첫 1.5초 대형 텍스트 + 반투명 배경 박스 (drawtext)
- [ ] **CTA 엔드카드**: 마지막 3초 구독/댓글 유도 텍스트
- [ ] 동적 페이싱: HOOK 1.5~3초, PROBLEM 5~8초, SOLUTION 8~15초, CTA 2~3초
- [ ] 루프 구조: 마지막 0.5초 ↔ 첫 0.5초 크로스페이드

### A-5. 탭 자동 갱신 (즉시 적용)
> 파일: main.py, 각 패널 수정

- [ ] main.py 탭 변경 이벤트 → 각 패널 `refresh()` 자동 호출
- [ ] image_panel.py: 탭 진입 시 DB 최신 스크립트 + 이미지 폴더 재스캔
- [ ] shorts_panel.py: 탭 진입 시 이미지/오디오/영상 최신 상태
- [ ] asset_browser.py: 탭 진입 시 자동 재로드

---

## 🟠 Phase B — TTS 혁명 (GPU: Qwen3-TTS)

> Edge TTS → ElevenLabs급 자연스러움. 채널 고유 내레이터 목소리 확립.
> RTX 4070 8GB GPU 필수.

### B-1. Qwen3-TTS 1.7B 로컬 백엔드
> 파일: 신규 `app/services/gpu_tts_service.py`

- [ ] `pip install transformers torch torchaudio` (CUDA 12.x)
- [ ] HuggingFace에서 Qwen3-TTS 1.7B 모델 다운로드
- [ ] `GpuTtsService` 클래스:
  - 모델 로드/언로드 (VRAM ~6GB)
  - `generate(text, emotion, speed, reference_audio)` → WAV
  - 단어별 타임스탬프 추출 → `[{word, start_ms, end_ms}]` JSON
  - VRAM 관리: 생성 후 `torch.cuda.empty_cache()`
- [ ] 감정 제어: 나레이션(기본), 분노, 슬픔, 흥분, 속삭임
- [ ] 음성 클로닝: 참조 오디오 3~15초로 "무협 전문 내레이터" 생성
- [ ] 한국어 최적화 테스트 (억양, 호흡, 포즈 자연스러움 검증)

### B-2. GPT-SoVITS v3 비교 테스트 (선택)
- [ ] GPT-SoVITS v3 설치 (407M 파라미터, ~4GB VRAM, 한국어 지원)
- [ ] 동일 텍스트로 Qwen3-TTS vs GPT-SoVITS 블라인드 비교
- [ ] 최종 메인 모델 선택 (나머지는 폴백)

### B-3. TTS 통합 인터페이스
> 파일: tts_service.py 수정

- [ ] `engine` 파라미터: `"edge"` | `"qwen3"` | `"sovits"`
- [ ] Edge TTS: 기본 폴백, GPU 없을 때, 빠른 미리듣기
- [ ] Qwen3-TTS: 최종 렌더링, 고품질, 감정 제어
- [ ] GPT-SoVITS: 음성 클로닝 특화 (선택)
- [ ] UI 토글: tts_test.py, shorts_panel.py에 엔진 선택 드롭다운

### B-4. 단어별 타임스탬프 → 자막 연동
- [ ] Qwen3-TTS word_timestamps → JSON 저장
- [ ] Edge TTS WordBoundary 이벤트도 동일 포맷 추출
- [ ] Phase A-2 PyCaps 키네틱 자막과 정밀 동기화

### B-5. ComfyUI TTS 워크플로우 (선택적)
- [ ] ComfyUI 오디오 노드로 Qwen3-TTS 실행 워크플로우 구성
- [ ] 이미지 생성 → TTS → 비디오 클립을 단일 ComfyUI 파이프라인으로 연결 가능성 검토

---

## 🟡 Phase C — 이미지 혁명 (캐릭터 일관성 + 고품질)

> 캐릭터가 장면마다 바뀌는 문제 해결. FLUX Kontext + 업스케일.
> API + ComfyUI 혼합 전략.

### C-1. FLUX Kontext 캐릭터 일관성 시스템
> 파일: image_generator.py 대폭 수정

- [ ] **캐릭터 참조 이미지 시스템**:
  - 스크립트당 "캐릭터 시트" 1장 생성 (정면 상반신 + 이름)
  - 이후 모든 장면에서 참조 이미지를 `image_url` 파라미터로 전달
  - Pollinations.ai `kontext` 모델 사용 (유료 $0.04/장)
- [ ] **ComfyUI FLUX Kontext 워크플로우** (로컬 대안):
  - FLUX Kontext Dev GGUF 모델 다운로드
  - ComfyUI에서 참조 이미지 → 새 장면 생성 워크플로우 구성
  - Python API로 헤드리스 실행: `POST http://localhost:8188/prompt`
  - 워크플로우 JSON 템플릿 저장 (`static/comfyui_workflows/kontext_character.json`)
- [ ] 성별 자동 감지 (`_detect_gender`) + 키워드 자동 삽입
- [ ] negative_prompt 기본값: `"deformed hands, extra fingers, blurry face, text, watermark, logo, low quality, bad anatomy, extra limbs"`
- [ ] 성별별 negative 확장

### C-2. Real-ESRGAN 4x 업스케일
> 파일: 신규 `app/services/upscale_service.py`

- [ ] **ComfyUI Real-ESRGAN 워크플로우**:
  - `RealESRGAN_x4plus_anime_6B` 모델 (만화/애니 특화)
  - `RealESRGAN_x4plus` 모델 (리얼리스틱)
  - ComfyUI 워크플로우 JSON 템플릿 (`static/comfyui_workflows/upscale_4x.json`)
  - 생성 이미지 → ComfyUI API → 4x 업스케일 이미지 반환
- [ ] **독립 Python 폴백** (ComfyUI 없이):
  - `pip install realesrgan`
  - GPU 로컬 실행 (~1GB VRAM, 다른 작업과 병행 가능)
- [ ] image_generator.py에서 생성 후 자동 업스케일 파이프라인
- [ ] 장르별 업스케일 모델 자동 선택: 무협/애니 → anime_6B, 기타 → x4plus

### C-3. 멀티모델 폴백 체인
- [ ] 모델 우선순위: `kontext` → `flux` → `imagen-4` → `gptimage` → `dirtberry` → Together.ai FLUX Schnell
- [ ] 모델 실패 시 자동 다음 모델 (최대 3회), 성공 모델 로깅
- [ ] 모델별 파라미터 맵 (negative_prompt 지원 여부 등)
- [ ] Together.ai 외부 폴백: `TOGETHER_API_KEY` 환경변수

### C-4. 이미지 품질 자동 검증
- [ ] 후보 2~3장 생성 → 최적 선택 로직
- [ ] 검증 기준: 해상도 확인, 검은 화면/흰 화면 감지, 텍스트 워터마크 감지
- [ ] 불합격 이미지 자동 재생성

### C-5. 동시 요청 제한 & 큐
- [ ] `asyncio.Semaphore(2)` — 최대 2개 동시 요청
- [ ] 모델별 rate limit: kontext 10초, flux 8초, imagen-4 5초
- [ ] 진행률 로깅: "대기 N건, 진행 M건, 완료 K건"

---

## 🟢 Phase D — SFX/BGM 서비스

> 무음 = 이탈. 3-layer 오디오의 BGM/SFX 소스 확보.
> GPU 시분할 + API 혼합.

### D-1. Stable Audio Open 로컬 생성 (GPU)
> 파일: 신규 `app/services/sfx_service.py`

- [ ] Stable Audio Open 1.0 모델 (~4GB VRAM)
- [ ] GPU 시분할: Qwen3-TTS 언로드 → Stable Audio 로드 → 생성 → 언로드
- [ ] `SfxService.generate_bgm(mood, genre, duration_sec)` → WAV
- [ ] `SfxService.generate_sfx(description, duration_sec)` → WAV
- [ ] 최대 47초, 숏츠 BGM에 적합

### D-2. 외부 API 폴백
- [ ] ElevenLabs Sound Effects API (무료 10,000자/월): 텍스트 → 효과음
- [ ] Freesound.org API (무료, CC0): 키워드 검색 → 다운로드
- [ ] 폴백: Stable Audio → ElevenLabs → Freesound → 기본 BGM 파일

### D-3. 스크립트 태그 자동 연동
- [ ] `[SFX: 검 부딪히는 소리]` → sfx_service 호출 → 타임코드에 삽입
- [ ] `[BGM: 긴장감 있는 무협]` → bgm 생성 → 전체 트랙 깔기
- [ ] shorts_maker.py / video_assembler.py에 Phase A-3 오디오 믹싱 연동

---

## 🔵 Phase E — ComfyUI 통합 허브

> ComfyUI를 이미지/비디오/업스케일의 **통합 로컬 엔진**으로 활용.
> API 방식 호출로 기존 NiceGUI 파이프라인과 연결.

### E-1. ComfyUI 서버 설정
> 파일: 신규 `app/services/comfyui_service.py`

- [ ] ComfyUI 설치 + 자동 시작 스크립트
- [ ] 헤드리스 모드 실행: `python main.py --listen 0.0.0.0 --port 8188`
- [ ] `ComfyUIService` 클래스:
  - `submit_workflow(workflow_json, inputs)` → job_id
  - `poll_result(job_id)` → 이미지/비디오 파일 경로
  - WebSocket 기반 실시간 진행률 모니터링
  - 에러 핸들링 + 타임아웃 (300초)
- [ ] 워크플로우 JSON 템플릿 관리: `static/comfyui_workflows/` 디렉토리

### E-2. ComfyUI 워크플로우 라이브러리
- [ ] **이미지 생성**: FLUX Kontext Dev + 참조 이미지 → 캐릭터 일관성 이미지
  - 템플릿: `kontext_character.json`
  - 입력: 참조 이미지 경로, 프롬프트, negative_prompt, 해상도
- [ ] **이미지 업스케일**: Real-ESRGAN 4x (anime_6B / x4plus)
  - 템플릿: `upscale_4x.json`
  - 입력: 이미지 경로, 모델 선택
- [ ] **이미지→비디오**: Wan 2.2 5B GGUF (8GB VRAM 최적화)
  - 템플릿: `wan_i2v.json`
  - 입력: 이미지 경로, 모션 프롬프트, 길이(초), 해상도(480p)
  - 생성 후 Real-ESRGAN으로 프레임별 업스케일 (선택)
- [ ] **통합 파이프라인**: 이미지 생성 → 업스케일 → 비디오 변환을 단일 워크플로우로
  - 템플릿: `full_pipeline.json`

### E-3. ComfyUI ↔ NiceGUI 연동
- [ ] image_panel.py: "ComfyUI로 생성" 버튼 추가 (API 호출 → 결과 이미지 표시)
- [ ] shorts_panel.py: "AI 클립 생성" 버튼 (이미지 → Wan 비디오 클립)
- [ ] asset_browser.py: ComfyUI 생성 파일도 스캔 범위에 포함
- [ ] 설정 패널: ComfyUI 서버 주소/포트 구성, 연결 상태 표시

### E-4. ComfyUI 모델 관리
- [ ] 필요 모델 자동 다운로드 스크립트:
  - FLUX Kontext Dev GGUF (~10GB)
  - Wan 2.2 5B GGUF Q5 (~8GB)
  - RealESRGAN_x4plus_anime_6B (~70MB)
  - RealESRGAN_x4plus (~67MB)
- [ ] 모델 존재 여부 확인 → 없으면 다운로드 안내
- [ ] VRAM 사용량 모니터링 (torch.cuda.memory_allocated)

---

## 🟣 Phase F — 파이프라인 통합 & 자동화

### F-1. 원클릭 파이프라인
- [ ] 스크립트 선택 → TTS → 이미지 → (비디오 클립) → 자막 → 오디오 믹싱 → 최종 영상
- [ ] 프로그레스바: 각 단계별 진행률 실시간 표시
- [ ] 에러 발생 시 해당 단계만 재시도

### F-2. 품질 체크리스트 자동 검증
- [ ] 생성 전 확인:
  - 첫 1.5초 훅 텍스트 있는가?
  - 이미지 3장 이상인가?
  - 나레이션 200자 이내인가? (숏츠)
  - CTA 문장 포함?
  - 예상 길이 15~59초? (숏츠)
- [ ] 생성 후 확인:
  - 영상 해상도 1080x1920 (숏츠)?
  - 오디오 3-layer?
  - 자막 존재?

### F-3. 각 탭 format 필터링
- [ ] 이미지 탭: format=shorts → 9:16 기본
- [ ] 숏츠 탭: shorts 스크립트만 표시
- [ ] 영상 탭: long 스크립트만 표시

### F-4. 훅 자동 생성기
- [ ] 10가지 훅 템플릿 DB화
- [ ] LLM이 훅 3개 후보 생성 → 사용자 선택 → 자동 적용

### F-5. YouTube 업로드
- [ ] YouTube Data API v3 연동
- [ ] SEO 메타데이터 자동 생성
- [ ] 채널별 피크 시간 자동 스케줄링

---

## 🐛 기존 이슈 (틈틈이)

- [ ] Reddit User-Agent 수정 (trend_scout.py)
- [ ] LLM JSON 파싱 안정화 (try/except + 재시도)
- [ ] NiceGUI 이벤트 API 통일
- [ ] Git에서 바이너리 이미지 제거 → .gitignore
- [ ] 테스트 코드 정리 (test_*.py → tests/)

---

## 📊 Phase별 예상 등급 변화

| 영역 | 현재 | Phase A 후 | Phase B 후 | Phase C 후 | Phase D 후 | Phase E+F 후 |
|---|---|---|---|---|---|---|
| 영상 | D+ | **B+** | A- | A- | A | **A** |
| 자막 | D | **B+** | **A** | A | A | **A** |
| TTS | C- | C- | **A** | A | A | **A** |
| 이미지 | D | D+ | D+ | **A** | A | **A** |
| SFX/BGM | 없음 | B- | B | B | **A** | **A** |
| 스크립트 | B- | B | B+ | B+ | A- | **A** |
| 파이프라인 | C | C+ | B | B+ | A- | **A** |
| **YT 안전성** | ❌ slop | **✅ 탈출** | ✅ | ✅ | ✅ | **✅ 프로** |

---

## 🖥️ GPU 시분할 + ComfyUI 계획 (RTX 4070 8GB)


[Phase A] 이미지 → Pollinations API (GPU 불필요) 비디오 클립 → Pollinations API 또는 ComfyUI Wan 2.2 GGUF (8GB) 자막/SFX → CPU + FFmpeg ↓ [Phase B] TTS → Qwen3-TTS 1.7B (~6GB VRAM) → 생성 → 언로드 ↓ torch.cuda.empty_cache() [Phase C] 이미지 → ComfyUI FLUX Kontext GGUF (8GB) → 생성 → 언로드 업스케일 → ComfyUI Real-ESRGAN (~1GB) → 병행 가능 ↓ torch.cuda.empty_cache() [Phase D] SFX/BGM → Stable Audio Open (~4GB) → 생성 → 언로드 ↓ [Phase E] ComfyUI 통합 파이프라인: FLUX Kontext → Real-ESRGAN → Wan I2V → 순차 실행 (단일 ComfyUI 워크플로우로 연결 가능)


---

## 📦 필요 설치 패키지 (전체)

```bash
# Phase A
pip install pycaps                    # 키네틱 자막

# Phase B
pip install transformers torch torchaudio  # Qwen3-TTS

# Phase C
pip install realesrgan                # 이미지 업스케일 (ComfyUI 없이 독립 사용 시)

# Phase E — ComfyUI
git clone https://github.com/comfyanonymous/ComfyUI.git
cd ComfyUI && pip install -r requirements.txt
# 모델 다운로드:
#   models/unet/    → flux-kontext-dev-GGUF, wan2.2-5b-GGUF
#   models/upscale/ → RealESRGAN_x4plus_anime_6B.pth, RealESRGAN_x4plus.pth

🔢 버전 계획
버전	Phase	주요 내용
v1.8	A-1~A-3	AI 비디오 클립, 키네틱 자막, 3-layer 오디오
v1.9	A-4~A-5	시각효과 12종, 탭 자동갱신, 훅/CTA 오버레이
v2.0	B	Qwen3-TTS 통합, 채널 전용 음성, 타임스탬프 자막 동기화
v2.1	C	FLUX Kontext 캐릭터 일관성, Real-ESRGAN 4x, 멀티모델
v2.2	D	Stable Audio BGM, ElevenLabs SFX, 태그 자동 연동
v3.0	E+F	ComfyUI 통합, 원클릭 파이프라인, YouTube 업로드, 품질 체크
